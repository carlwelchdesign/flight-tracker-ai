# M4.1 — Public Decision Intelligence and Exploration

Default owner: Full-stack product engineering, with product design, aviation
weather, accessibility, and trust review.

## Delivery boundary

This sequence begins after the public FT-404 deployment is complete. FT-403
neutral validation runs after FT-413 through FT-416 so it evaluates the finished
recruiter experience. This milestone turns the
public tracker from a strong map demonstration into an explainable
decision-intelligence experience before M5 explores recommendations or LLM
drafting.

Each ticket uses one dedicated feature branch, intentional ticket-scoped
commits, one pull request targeting `main`, passing required checks, and
recorded browser evidence. No ticket may expose protected tenant data, raw
provider payloads, operational controls, or unsupported flight facts.

## FT-413 — Explain why a selected flight needs attention

Status: Complete

Branch: `feat/ft-413-public-attention-explanation`
Implementation commit: `85afab1`
Merge commit: `2ed8268`
Pull request: [#39](https://github.com/carlwelchdesign/flight-tracker-ai/pull/39)
Owner: Full-stack product engineering and product design

Add a compact, read-only explanation to the selected-aircraft panel so a public
viewer can understand the evidence, deterministic reasoning, uncertainty, and
source freshness behind an attention state without signing in.

Dependencies: FT-404

Acceptance checklist:

- [x] A selected deterministic replay aircraft demonstrates at least one
      meaningful attention state on every supported viewport.
- [x] The explanation separates observed or replay facts, deterministic rule
      results, and geometric estimates; it never describes an estimate as a
      filed route, destination, ETA, or provider observation.
- [x] The panel shows a readable score or priority breakdown including the
      evidence that contributed, source times, freshness, and rule version.
- [x] Live aircraft without the required route/hazard evidence show an honest
      unavailable or not-evaluated state instead of an invented assessment.
- [x] Public responses exclude tenant identifiers, protected alert IDs, raw
      provider payloads, dispatcher actions, notes, and protected evidence URLs.
- [x] The explanation is contextual to the selected aircraft and does not
      restore a marketing banner or walkthrough above the tracker.
- [x] Loading, empty, replay, live, stale, unavailable, and partial-evidence
      states are designed and tested.
- [x] Keyboard and screen-reader users can reach the explanation and understand
      the score without relying on color or map geometry alone.
- [x] Component, Rust contract, authorization, and sanitized-public-boundary
      tests pass.
- [x] Desktop and mobile browser verification proves selection changes the
      explanation and that the public page exposes no operational action.
- [x] Branch, commits, PR, CI results, screenshots, hosted promotion, and
      verification evidence are recorded before completion.

Success signal: In the FT-403 follow-up, a neutral reviewer can identify which
demonstration flight needs attention and explain why without facilitator help.

Verification evidence:

- Rust derives the replay explanation from the checked-in M1 scenario through
  the existing route-hazard rule and scoring boundaries. The public contract
  tests prove protected identifiers, raw payloads, operator actions, and audit
  fields are absent.
- Web component, parser, proxy-route, and public-route tests cover the scored
  FT303 replay state, honest live and replay non-evaluation, bounded parsing,
  credential-free forwarding, and fail-closed upstream behavior.
- Local browser verification at the desktop viewport selected FT303 and showed
  its 85/100 critical priority, four score contributions, rule and score
  versions, replay facts, geometric-estimate disclaimer, and source times with
  no sign-in or operational action.
- A 390 by 844 mobile browser check selected the replay using the public control,
  exposed the same score and evidence before the aircraft facts, reported a
  390-pixel document width with no horizontal overflow, and found no application
  error overlay, sign-in invitation, or operational action.
- PR #39 passed Rust, web, API/PostGIS, and Vercel checks before merge. Render
  staging deploy `dep-d9g81f4m0tmc73bg0360` and production deploy
  `dep-d9g83fr7uimc73ej187g` run merge commit `2ed8268`; both services reported
  live, and staging then production passed health and readiness checks.
- Vercel deployment `EdPWupvYT2BNuWhFuXyn78KPKtpy` was promoted to
  `flight-tracker-ai-one.vercel.app`. Its public proxy returned FT303 as
  `critical-85`, and a hosted browser selected Replay demo and exposed the
  complete score, replay facts, rule result, estimate limits, and matching
  aircraft telemetry without sign-in or operational actions.
- The production sanitized-public-boundary verifier passed the public tracker,
  security headers, API health/readiness, and unauthenticated protected-route
  denial. A separate contract probe found none of the forbidden protected
  field names in the public attention response.

## FT-414 — Add a replay time machine and aircraft telemetry charts

Status: Complete

Branch: `feat/ft-414-flight-time-machine`
Implementation commit: `2baab01`
Merge commit: `4fa15e6`
Pull request: [#41](https://github.com/carlwelchdesign/flight-tracker-ai/pull/41)
Owner: Frontend and full-stack product engineering

Let a viewer play, pause, scrub, and replay a bounded demonstration timeline
while the selected aircraft's map position and accessible altitude, speed, and
heading history remain synchronized.

Dependencies: FT-413

Acceptance checklist:

- [x] Replay offers play, pause, restart, scrub, and a small allowlist of speed
      controls with explicit UTC scenario time.
- [x] Map position, observed/replay trail, attention explanation, and telemetry
      charts update from one deterministic virtual clock.
- [x] Altitude, ground speed, and heading charts include units, timestamps,
      focus states, accessible names, and a textual data summary.
- [x] Replay controls are clearly distinguished from live traffic; live ADS-B
      history remains bounded to page memory under ADR-012 and is never
      persisted, exported, logged, or sent to an LLM.
- [x] Scrubbing is deterministic and does not create duplicate alerts, reorder
      evidence incorrectly, or mutate protected operational state.
- [x] Reduced-motion mode replaces nonessential animation while preserving
      time-machine controls and understandable state changes.
- [x] Loading, no-history, single-point, paused, complete, and replay-reset
      states are covered.
- [x] Component and integration tests prove clock synchronization, chart
      calculations, keyboard operation, reduced motion, and reload reset.
- [x] Desktop and mobile browser verification covers scrubbing, playback,
      aircraft selection, charts, weather layers, and no horizontal overflow.
- [x] Branch, commits, PR, CI results, screenshots, hosted promotion, and
      verification evidence are recorded before completion.

Success signal: A first-time viewer can replay the scenario and identify when
the aircraft's attention state changed without needing instructions.

Verification evidence:

- Rust builds the public timeline from the checked-in replay scenario, caps it
  at 15 minutes and 100 observations, and excludes operator, flight, route,
  hazard, alert, provider-record, raw-payload, dispatcher, and audit fields.
- The typed browser boundary rejects malformed, out-of-order, and unbounded
  timelines. Deterministic interpolation tests prove the clock produces the
  same aircraft picture and trail for the same elapsed time.
- Component and integration tests cover transport controls, the bounded speed
  allowlist, scrubbing across the attention threshold, accessible chart text,
  no-history, single-point, unavailable, live, and replay states.
- Local desktop and 390 by 844 mobile browser checks proved play advances the
  clock, pause holds it, restart returns to zero, completion stops playback,
  scrubbing restores FT303's explanation, the map renders a labeled replay
  trail, three telemetry charts render, and the mobile document has no
  horizontal overflow. The reduced-motion browser retained every control while
  collapsing marker transition time.
- PR #41 passed Rust, web, API/PostGIS, and Vercel checks before merge. Render
  staging deploy `dep-d9g8glbrjlhs73bu3hfg` and production deploy
  `dep-d9g8imjrjlhs73bu753g` run merge commit `4fa15e6`; both reported live and
  passed health and readiness checks.
- Vercel deployment `dpl_8X2CfSY87uCx9VeyZgRfk3PNAMiR` was assigned to
  `flight-tracker-ai-one.vercel.app`. Its hosted public proxy returned the
  three-minute scenario, seven bounded observations, and the 0.5x, 1x, and 2x
  playback allowlist with no protected field categories.
- Hosted browser verification opened Replay demo at 16:01 UTC, rendered the
  replay trail and three accessible telemetry charts, advanced playback from
  60 to 61 seconds, and held the paused clock. It found no sign-in invitation,
  application error overlay, horizontal overflow, or browser console error.
- The production sanitized-public-boundary verifier passed the public tracker,
  security headers, API health/readiness, and unauthenticated protected-route
  denial. Local desktop and mobile screenshots record the synchronized tracker
  layout and reduced-motion behavior.

## FT-415 — Add aircraft search and shareable tracker URLs

Status: Complete

Branch: `feat/ft-415-search-shareable-views`
Final commit: `62e4d0b` (merged as `ecfec77`)
Pull request: [#43](https://github.com/carlwelchdesign/flight-tracker-ai/pull/43)
Owner: Frontend and full-stack product engineering

Make an aircraft or curated scenario easy to find, revisit, and share while
keeping public query parameters bounded and resilient to expiring live data.

Dependencies: FT-414

Acceptance checklist:

- [x] Search filters only the current sanitized regional picture by displayed
      callsign or public ICAO identifier; it does not create an arbitrary
      provider-search proxy.
- [x] Empty, no-match, stale, replay, and unavailable search states are clear,
      keyboard accessible, and easy to reset.
- [x] The URL can encode an allowlisted region, selected aircraft or replay
      scenario, replay time, map view, visible weather layers, and wind level.
- [x] Invalid, oversized, unknown, or stale URL state is normalized safely and
      degrades to a useful default without a crash or redirect loop.
- [x] A shared live-aircraft link explains when the aircraft has left the
      current snapshot and preserves the region and view instead of inventing
      retained history.
- [x] Copy-link and supported native-share actions provide visible success and
      failure feedback with a clipboard fallback.
- [x] URLs never contain tenant IDs, protected alert IDs, raw provider fields,
      secrets, personal data, or dispatcher state.
- [x] Navigation, refresh, back/forward history, and direct-link hydration are
      covered by component and browser tests.
- [x] Desktop and mobile browser verification proves search, direct links,
      layer restoration, stale-link recovery, and keyboard operation.
- [x] Branch, commits, PR, CI results, screenshots, hosted promotion, and
      verification evidence are recorded before completion.

Success signal: A reviewer can open a supplied URL and reach the intended
region, scenario or aircraft, time, and layer configuration without setup.

Verification evidence:

- A pure URL boundary caps query length, allowlists regions, the portfolio
  scenario, map bounds, weather layers, and wind levels, and serializes only a
  displayed callsign, validated six-character ICAO hex, or registration as the
  aircraft key. Malformed state normalizes to the useful public default without
  redirecting.
- The Rust public-position boundary derives that ICAO hex only from a validated
  provider identifier and still excludes the raw provider record. Rust and web
  contract tests prove invalid identifiers fail closed.
- Component tests cover direct replay hydration, selected aircraft and replay
  time, map and weather restoration, no-match search, expired live-aircraft
  recovery, region preservation, clipboard feedback, and popstate navigation.
- Local desktop and 390 by 844 mobile browser checks opened the same direct
  FT303 link at 60 seconds, restored the custom map view and 300 hPa layer set,
  filtered to FT101, showed an honest no-match state, selected FT101, navigated
  back to FT303, and copied the exact restored URL.
- The same browser checks opened an expired `UAL404` link in the LAX region,
  preserved its map view, explained that the aircraft had left the snapshot,
  and cleared only the aircraft key. Both viewports had no application error,
  sign-in invitation, or horizontal overflow.
- Full local verification passes: 126 Rust tests across unit, contract,
  resilience, geometry, and schema suites; Rust format and strict Clippy; 128
  web tests; lint; TypeScript; and the production Next.js build.
- PR #43 passed Rust, web, API/PostGIS, and Vercel checks before merge. Render
  staging deploy `dep-d9g91rbrjlhs73bv07a0` and production deploy
  `dep-d9g93l6pbkes73896nfg` run merge commit `ecfec77`; both reported live and
  passed health and readiness checks.
- Vercel deployment `dpl_HZuC93aW4EXbogENPj9trRjUPKLG` was assigned to
  `flight-tracker-ai-one.vercel.app`. Production browser verification restored
  the direct FT303 replay link at 60 seconds with the custom map view, selected
  weather layers, and 300 hPa winds; search filtered to FT101 and produced the
  explicit no-match state without a sign-in invitation or application error.
- Production SFO traffic returned a current public picture with validated ICAO
  hex identifiers and no raw provider record IDs. The sanitized-public-boundary
  verifier passed the tracker, security headers, API health/readiness, and
  unauthenticated protected-route denial with `publication_ready: true`.

## FT-416 — Add airport TAF and nearby PIREP intelligence

Status: Complete

Branch: `feat/ft-416-airport-taf-pirep-intelligence`
Final commits: `6df623c`, hosted-route fix `cc3f591` (merged as `5d789a2`)
Pull requests: [#45](https://github.com/carlwelchdesign/flight-tracker-ai/pull/45), [#46](https://github.com/carlwelchdesign/flight-tracker-ai/pull/46)
Owner: Backend, frontend, and aviation-weather engineering

Turn airport markers into inspectable forecast and nearby pilot-report entry
points using bounded, source-attributed NOAA Aviation Weather Center data.

Dependencies: FT-415

Acceptance checklist:

- [x] Rust owns fixed NOAA Aviation Weather Center TAF and PIREP requests for
      the curated regions or allowlisted airports; the browser cannot submit
      arbitrary coordinates, station lists, time ranges, or upstream URLs.
- [x] Upstream cadence, result count, timeout, response size, and in-memory
      caching respect the official API guidance and prevent request stampedes.
- [x] The sanitized public model exposes only fields required for forecast and
      nearby-report presentation, with source, issue/valid times, receive time,
      and freshness or unavailable state.
- [x] Selecting an airport opens a forecast timeline that distinguishes current
      METAR observations from TAF forecast periods and conditional changes.
- [x] Nearby PIREPs show report time, distance, altitude, report type, and
      available turbulence, icing, cloud, wind, or temperature evidence without
      implying complete coverage or current conditions at an aircraft.
- [x] PIREPs are never silently attributed to a selected flight, and missing or
      ambiguous location/altitude data is handled explicitly.
- [x] TAF and PIREP failures remain independent from aircraft, METAR, SIGMET,
      radar, satellite, and model-wind layers; the last accepted bounded picture
      is labeled retained rather than current.
- [x] Provider source links, timestamps, advisory portfolio wording, and
      non-operational limits remain visible.
- [x] Parser fixtures and Rust/web tests cover malformed, empty, rate-limited,
      stale, retained, and partial upstream responses plus unit/time handling.
- [x] Desktop and mobile browser verification covers airport selection, a TAF
      timeline, nearby PIREPs when available, unavailable fallback, and weather
      layer coexistence.
- [x] Branch, commits, PR, CI results, screenshots, hosted promotion, and
      verification evidence are recorded before completion.

Success signal: A reviewer can select a regional airport and explain the
difference between its current observation, terminal forecast, and nearby
pilot-reported conditions.

Verification evidence:

- The Rust boundary owns one fixed seven-airport TAF request and one fixed,
  three-hour continental-U.S. PIREP bounding-box request. The browser can send
  only one of seven airport codes; arbitrary boxes, coordinates, time ranges,
  upstream URLs, and unknown airports never reach NOAA.
- Separate 10-minute TAF and 60-second PIREP caches match NOAA's published
  update cadence, hold one refresh lock to prevent stampedes, cap upstream
  bodies at 2 MiB, apply bounded timeout and retry behavior, filter reports to
  100 NM, and expose at most 20 nearby reports.
- The public model excludes raw TAF text, raw PIREP text, provider record IDs,
  aircraft association, and ambiguous unlocated reports. It preserves source,
  issue, validity, report, receive, distance, altitude, unit, and freshness
  evidence with explicit retained, partial, and unavailable states.
- The map's curated regional-airport entry opens a responsive TAF timeline and
  nearby PIREP list. Copy explains that reports are sparse, voluntary, may omit
  location or altitude, do not prove absence of conditions, and are not
  attributed to any selected flight.
- NOAA's live API specification and sample responses were checked against the
  implemented query shape. Full local verification passes 131 Rust tests,
  formatting, and strict Clippy plus 136 web tests, lint, TypeScript, and the
  production Next.js build.
- Local desktop and 390 by 844 mobile browser checks opened KSFO intelligence,
  rendered prevailing and 30-percent TEMPO periods beside a located PIREP,
  retained the existing replay, aircraft, map, and weather surface, showed the
  non-attribution coverage warning, and had no sign-in prompt, application
  error, or horizontal overflow. Component coverage also proves the independent
  unavailable fallback; desktop and mobile screenshots record both layouts.
- PR #45 and hosted-route correction PR #46 passed Rust, web, API/PostGIS, and
  Vercel checks before merge. Render staging deploy
  `dep-d9g9fnurnols73c2sb60` and production deploy
  `dep-d9g9jqjtqb8s7389pkp0` run merge commit `8e462be`; both reported live and
  passed health/readiness checks.
- Staging live-provider verification returned five KSFO TAF periods and seven
  KDEN periods plus two KDEN PIREPs. Both feeds were current, raw TAF, raw
  PIREP, and provider-record fields were absent, and an unknown airport returned
  400 without creating an upstream query.
- Vercel deployment `dpl_CDuv8qMbVnPLugP39Hbapp2XGp6Z` was assigned to
  `flight-tracker-ai-one.vercel.app`. Hosted browser verification showed KDEN's
  seven-period forecast with TEMPO and 30-percent probability changes, two
  distance/altitude/time/turbulence PIREPs, source attribution, and the explicit
  no-flight-attribution and advisory-use boundaries.
- The production sanitized-public-boundary verifier passed the tracker,
  security headers, API health/readiness, and unauthenticated protected-route
  denial with `publication_ready: true` after the final public-route promotion.

## M4.1 completion gate

- [x] FT-413 through FT-416 are complete and merged independently.
- [ ] A neutral follow-up reviewer completes the public explanation, replay,
      search/share, and airport-intelligence tasks without facilitator help.
- [x] Public authorization probes still prove that operational controls,
      tenant data, raw evidence, and audit history remain protected.
- [x] Production verification proves live, replay, stale, unavailable, reduced
      motion, desktop, and mobile behavior with no application errors.
- [x] The M5 recommendation and LLM boundaries remain unchanged: deterministic
      rules decide eligibility and severity, and humans approve generated work.
